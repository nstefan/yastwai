# Ralph Wiggum Method - Index

**Full content**: `agents/reference/ralph-wiggum-full.md`

## Key Concepts (search reference for details)
- Three-phase workflow: Define Requirements → Plan → Build
- Loop mechanics: Fresh context each iteration
- Upstream steering (patterns) + Downstream steering (tests)
- File structure: PROMPT.md, AGENTS.md, IMPLEMENTATION_PLAN.md, specs/

## Search Examples
```
grep -r "Loop Mechanics" agents/reference/
grep -r "Steering" agents/reference/
grep -r "IMPLEMENTATION_PLAN" agents/reference/
grep -r "backpressure" agents/reference/
```
